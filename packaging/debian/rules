#!/usr/bin/make -f

export PYTHONDONTWRITEBYTECODE=1

%:
	dh $@ --with python3

override_dh_auto_build:
	$(MAKE) ui

override_dh_auto_test:
	python3 -c 'import sqlite3'
	python3 -m unittest -v
	cd ui && npm test

override_dh_auto_install:
	$(MAKE) install DESTDIR=debian/fan-control PREFIX=/usr
